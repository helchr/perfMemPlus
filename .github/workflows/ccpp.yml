name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: run build scripts
      run: | 
        sudo ./build-profiler.sh
        sudo ./build-viewer.sh
